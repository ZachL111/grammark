# Review Journal

I treated `grammark` as a project where the smallest useful behavior should still be inspectable.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its developer tools focus without claiming live deployment or external usage.

## Cases

- `baseline`: `change width`, score 138, lane `watch`
- `stress`: `diagnostic quality`, score 159, lane `ship`
- `edge`: `review cost`, score 183, lane `ship`
- `recovery`: `safe rewrite`, score 182, lane `ship`
- `stale`: `change width`, score 162, lane `ship`

## Note

The useful failure mode here is a wrong decision on a named case, not a vague style disagreement.
